# Variables de entorno para el backend INFOUNA

# Puerto local (Render asigna PORT automáticamente en producción)
PORT=4001

# Llave de NewsAPI (configurar en producción)
NEWS_API_KEY=your_newsapi_key_here

# Orígenes permitidos para CORS (separados por coma)
# Ejemplo: https://infouna-frontend.vercel.app, https://www.infouna.edu.pe
CORS_ORIGINS=http://localhost:5173, http://localhost:5181

# Configuración de base de datos
# En desarrollo local con Laragon:
#   DB_HOST=127.0.0.1
#   DB_USER=root
#   DB_PASSWORD=
#   DB_NAME=infouna
# En producción (Render) usar una base gestionada (PlanetScale/Railway) y pegar credenciales reales.
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=infouna

# Límite de conexiones del pool
DB_CONN_LIMIT=10

# TLS para proveedores que lo exigen (ej. PlanetScale)
# Si tu proveedor requiere SSL/TLS, habilítalo:
#   DB_SSL=true
#   DB_SSL_REJECT_UNAUTHORIZED=false
# Si no requiere TLS, deja DB_SSL sin definir o en false.
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false
